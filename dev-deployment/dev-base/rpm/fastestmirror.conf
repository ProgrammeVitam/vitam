[main]
enabled=0
verbose=0
always_print_best_host = true
socket_timeout=3
# Relative paths are relative to the cachedir (and so works for users as well as root).
hostfilepath=timedhosts.txt
maxhostfileage=10
maxthreads=15