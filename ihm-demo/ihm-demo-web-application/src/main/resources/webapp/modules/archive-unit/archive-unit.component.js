'use strict';

// Register `form` component, along with its associated controller and template
angular.
  module('archive.unit').
  component('archiveUnit', {
    templateUrl: 'archive-unit/archive-unit.template.html',
    controller: 'ArchiveUnitController'
  });
