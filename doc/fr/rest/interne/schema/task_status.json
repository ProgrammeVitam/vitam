{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "Task Status schema.",
  "description": "How to update a Task status.",
  "properties": {
    "task_status": {
      "type": "string",
      "enum": [
        "cancel",
        "commit",
        "pending"
      ]
    },
    "callback": {
      "type": "object",
      "title": "Callback schema.",
      "description": "Optional way for the client to specify a callback when the task is finished (could be passed during creation or during task_status=commit).",
      "properties": {
        "uri": {
          "type": "string",
          "description": "Uri for the callback containing the 2 possible callback replacements {id} and {status}."
        },
        "method": {
          "type": "string",
          "description": "Method in HTTP mode.",
          "name": "method",
          "enum": [
            "GET", "POST", "PUT", "PATCH", "OPTIONS", "HEAD"
          ]
        }
      }
    }
  }
}