Intégration de CA pour une offre swift
--------------------------------------

En cas d'utilisation d'une offre swift en https, il est nécessaire d'ajouter les :term:`CA` du certificat de l':term:`API` swift.

Il faut les déposer dans ``environments/certs/server/ca/`` avant de jouer le script ``./generate_keystores.sh``
