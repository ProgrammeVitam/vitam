jettyConfig: jetty-test.xml

# Exchange directory configuration (using /tmp for tests to avoid permission issues)
exchangeDirectory: /tmp/ina-tape-proxy-test/exchange
inaStorageDirectory: /tmp/ina-tape-proxy-test/storage

# Latency configuration (reduced for faster tests)
writeLatencyMs: 100
readLatencyMs: 100
loadLatencyMs: 50
unloadLatencyMs: 50
ejectLatencyMs: 50
roboticLatencyMs: 50
readTimeout: 100

# Concurrency configuration
nbDrives: 5