{
  "DSL": {
    "format": "object",
    "elements": {
      "$projection": {
        "format": "ref",
        "type": "PROJECTION",
        "hint": "Projections to apply on results"
      }
    }
  },
  "PROJECTION": {
    "format": "object",
    "elements": {
      "$fields": {
        "format": "anykey",
        "itemtype": {
          "format": "enum",
          "values": [
            1
          ]
        },
        "optional": true,
        "hint": "Fields to retrieve in the result"
      }
    }
  }
}
