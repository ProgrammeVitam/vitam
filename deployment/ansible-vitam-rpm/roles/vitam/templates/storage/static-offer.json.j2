[
{% for item in groups['hosts-storage-offer-default'] %}
{
    "id" : "{{item}}",
    "baseUrl" : "https://{{item}}:{{vitam_storageofferdefault_port_https}}",
    "parameters" : {
      "keyStore-keyPath": "{{vitam_folder_conf}}/keystore_storage.p12",
      "keyStore-keyPassword": "{{p12_storage_engine_password}}",
      "trustStore-keyPath": "{{vitam_folder_conf}}/truststore_storage.jks",
      "trustStore-keyPassword": "{{TrustStore_storage_engine_password}}"
    }
}
{% if not loop.last %},
{% endif %}
{% endfor %}
]
