---

- name: Install rpm
  yum:
    name: mongodb-org-server
    state: present

- name: Listen on all interface
  lineinfile:
    dest: /etc/mongod.conf
    regexp: "bindIp:"
    state: absent

- name: start mongodb
  service: 
    name: mongod
    enabled: true
    state: restarted
