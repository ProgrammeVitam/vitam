---
# tasks file for engine-external

# Question : move the scan-clamav.sh in /app ?
- name: change perms on scan-clamav.sh
  file:
    path: /vitam/conf/ingest-external/scan-clamav.sh
    mode: 0750

- name: installe epel-release
  yum:
    name=epel-release
    state=present

- name: install ClamAV packages
  yum:
    name=clamav,clamav-update,clamav-server,clamav-data,clamav-update,clamav-filesystem,clamav,clamav-scanner-systemd,clamav-devel,clamav-lib,clamav-server-systemd
    state=latest
