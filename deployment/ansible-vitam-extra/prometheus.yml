---

- hosts: hosts_prometheus
  roles:
    - role: prometheus-server
      tags: [tag_prometheus, tag_prometheus_server, generate_prometheus_conf, update_prometheus_configuration]

- hosts: hosts_alertmanager
  roles:
    - role: prometheus-alertmanager
      tags: [tag_prometheus, tag_alertmanager, update_prometheus_configuration]
