---

- include_tasks: "{{ item }}"
  loop:
    - grafana_server_enabled.yml
    - plugins.yml
    - provisionning.yml
  when: grafana.enabled == true

- include_tasks: grafana_server_disabled.yml
  when: grafana.enabled != true