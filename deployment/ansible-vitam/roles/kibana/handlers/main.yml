---

# handlers file for log-server
- name: "kibana - restart service"
  systemd:
    name: kibana
    state: restarted

- name: "kibana - reload consul configuration"
  service:
    name: vitam-consul
    state: reloaded
