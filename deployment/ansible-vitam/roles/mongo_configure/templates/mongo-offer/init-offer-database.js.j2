
// Switch to identity database
db = db.getSiblingDB('offer')

// Create indexes
// A single collection can have no more than 64 indexes.
db.OfferLog.createIndex( { "Container" : 1, "Sequence" : 1 } )

// Drop existing index container
db.OfferLog.dropIndex( { "container" : 1} )
db.OfferLog.dropIndex( { "Container" : 1} )
