{{ ansible_managed | comment }}

{{ mongo_folder_log }}/mongod.log {
        daily
        rotate {{ mongodb.history_days | default(30) }}
        copytruncate
        dateext
        dateformat -%Y-%m-%d
        dateyesterday
        extension .log
        compress
        delaycompress
        notifempty
        missingok
}
