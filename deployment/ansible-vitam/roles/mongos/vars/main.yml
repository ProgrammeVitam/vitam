---
mongo_tmp_path:    '{{ vitam_defaults.folder.root_path }}/tmp/mongos'
mongo_config_path: '{{ vitam_defaults.folder.root_path }}/conf/mongos'
mongo_folder_log:  '{{ vitam_defaults.folder.root_path }}/log/mongos'

service_at_boot: "{{ vitam_defaults.services.at_boot | default(false) }}"
