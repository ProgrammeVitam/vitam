{
    "id" : "default",
    "hot" : {
        "copy" : {{ vitam_strategy|length }},
        "offers" : [
            {% for item in vitam_strategy %}
            {"id" : "{{ item.name }}.service.{{ consul_domain }}"{% if item.referent is defined %}{% if item.referent|lower == "true" %}, "referent" : true{% endif %}{% endif %}}{% if not loop.last %},{% endif %}
            {% endfor %}
        ]
    }
}
