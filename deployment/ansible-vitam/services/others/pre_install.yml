---

- hosts: vitam
  gather_facts: no
  any_errors_fatal: true
  pre_tasks:
    - setup:
        filter: ansible_distribution
    - setup:
        filter: ansible_os_family       
  roles:
    - normalize-host
    - selinux_conf
    - syslogd
    - syslogd_configure
